# Tasks Module Custom Content

This markdown file is loaded for the Convex module named `tasks`.

It will be rendered *before* the module’s function list on `tasks.html`.

## Quick checklist

- Confirm it appears at the top of `tasks.html`
- Confirm other modules still render normally

