# Custom Landing Page

This content is loaded from `customization.contentPath`.

It renders on `index.html` before the rest of the default landing content.

---

## What to test

- Generate docs and verify this page shows up.
- Open `tasks.html`, `lists.html`, and the nested modules pages to confirm markdown injection works.
- Confirm missing/invalid markdown does not break generation (warnings only).

