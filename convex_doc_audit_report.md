Gap Analysis — Missing Features

1. **JSDoc Extraction (Critical):** Complex APIs require prose. Currently, descriptions have to be manually entered. ConvexDoc should parse the TypeScript source files (using the TS compiler API) to extract JSDoc comments preceding exported queries/mutations and auto-enrich the spec with descriptions.
